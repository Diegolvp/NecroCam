<Page x:Class="NecroCam.Views.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NecroCam.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Home">

    <Page.Resources>
        <LinearGradientBrush x:Key="Gradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1F2127" Offset="0.0"/>
            <GradientStop Color="#25292C" Offset="1.0"/>
        </LinearGradientBrush>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Border Background="{StaticResource Gradient}" Margin="20" CornerRadius="5" Padding="10">
                <Grid Margin="5" Background="{StaticResource Gradient}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Label Content="Status: " x:Name="LabelStatus" FontWeight="Medium" Foreground="White" FontSize="16"/>

                        <Ellipse Width="12" Height="12" Fill="Red" VerticalAlignment="Center" Margin="0,0,10,0" x:Name="StatusIndicator"/>

                        <TextBlock Text="Disconnect" FontSize="16" FontWeight="Medium" VerticalAlignment="Center" x:Name="StatusText" Foreground="Red"/>
                    </StackPanel>

                    <Button Grid.Column="1" Width="32" Height="32" ToolTip="Settings"></Button>


                </Grid>
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                </Border.Effect>
            </Border>

            <Border Background="{StaticResource Gradient}" Margin="20" CornerRadius="5" Padding="10">
                <Grid Margin="5" Background="{StaticResource Gradient}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <Label Content="Active Services " x:Name="LabelActiveServices" FontWeight="Medium" Foreground="White" FontSize="16"/>
                        <StackPanel Margin="0">
                            <Border Background="{StaticResource Gradient}" Margin="0" CornerRadius="5" Padding="10">
                                <Grid Margin="5" Background="{StaticResource Gradient}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Width="48" Height="48" Source="/Resources/obs-icon.png"/>
                                        <Label Content="OBS Web Socket" VerticalAlignment="Center" x:Name="LabelObsWebSocket" FontWeight="Medium" Foreground="White" FontSize="16"/>
                                    </StackPanel>
                                    <Ellipse Grid.Column="1" Width="12" Height="12" Fill="Red" VerticalAlignment="Center" Margin="0,0,10,0" x:Name="StatusObsWebSocket"/>

                                </Grid>
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>

                            <Border Background="{StaticResource Gradient}" Margin="0 15 0 0" CornerRadius="5" Padding="10">
                                <Grid Margin="5" Background="{StaticResource Gradient}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Width="48" Height="48" Source="/Resources/webcam-icon.png"/>
                                        <Label Content="Webcam Caption" VerticalAlignment="Center" x:Name="aff" FontWeight="Medium" Foreground="White" FontSize="16"/>
                                    </StackPanel>
                                    <Ellipse Grid.Column="1" Width="12" Height="12" Fill="Red" VerticalAlignment="Center" Margin="0,0,10,0" x:Name="StatusWebcamCaption"/>

                                </Grid>
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                            <Border Background="{StaticResource Gradient}" Margin="0 15 0 0" CornerRadius="5" Padding="10">
                                <Grid Margin="5" Background="{StaticResource Gradient}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Width="48" Height="48" Source="/Resources/monitor-icon.png"/>
                                        <Label Content="Virtual Cam" VerticalAlignment="Center" x:Name="LabelVirtualCam" FontWeight="Medium" Foreground="White" FontSize="16"/>
                                    </StackPanel>
                                    <Ellipse Grid.Column="1" Width="12" Height="12" Fill="Red" VerticalAlignment="Center" Margin="0,0,10,0" x:Name="StatusVirtualCam"/>

                                </Grid>
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                        </StackPanel>

                    </StackPanel>

                </Grid>
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.3"/>
                </Border.Effect>
            </Border>
        </StackPanel>

        <Button Margin="20 0" Grid.Row="1" Content="{Binding StartStopButtonText}" Foreground="White" Width="auto" Height="60"  
                FontSize="16"
                FontWeight="Medium"
                RenderTransformOrigin="0.5, 0.5" 
                Command="{Binding ToggleStartCommand}" 
                IsEnabled="{Binding IsButtonEnable}"
                Background="#F83C36">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="30">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>
</Page>
